class Message {
    constructor(text, photo, video, caption) {
      this.text = text;
      this.photo = photo;
      this.video = video;
      this.caption = caption;
    }
  }

  module.exports = Message;